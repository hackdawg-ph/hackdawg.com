version: '3.7'
services:
    web:
        build:
            args:
                APP_ENV: production
        restart: always

    nginx:
        restart: always
        ports:
            - '80:80'
            - '443:443'
        environment:
            DOMAIN: hackdawg.com
            EMAIL: hello@hackdawg.com

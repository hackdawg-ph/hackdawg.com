version: '3.7'
services:
    web:
        build:
            args:
                APP_ENV: local
        restart: unless-stopped
        volumes:
            - .:/var/www/html
            - ./deploy/php.ini:/usr/local/etc/php/conf.d/local.ini

    nginx:
        restart: unless-stopped
        command: sh -c "sed 's/{{NGINX_HOST}}/hackdawg.test/g' /etc/nginx/conf.d/hackdawg.template > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
        ports:
            - '80:80'
            - '443:443'
        volumes:
            - .:/var/www/html:ro
            - ./deploy/nginx/hackdawg.template:/etc/nginx/conf.d/hackdawg.template

    db:
        restart: unless-stopped
        volumes:
            - ./deploy/db/data:/var/lib/mysql
        ports:
            - '33069:3306'
        environment:
            MYSQL_DATABASE: hackdawg
            MYSQL_USER: hackdawg
            MYSQL_PASSWORD: password
            MYSQL_ROOT_PASSWORD: password
